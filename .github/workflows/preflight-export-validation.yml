name: Preflight Export Validation

on:
  workflow_dispatch:

jobs:
  preflight:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate export artifact script
        shell: pwsh
        run: |
          pwsh ./scripts/validate_export_artifact.ps1 -SolutionPath "legacy\archive\acsa\power-platform\exports\WSPTrainingManagementSystem_complete_solution.zip"
