# Demo Files

This folder contains convenience copies of the files needed on demo day.

- `WSPTrainingManagementSystem_complete_solution.zip` is the verified Power Platform solution package.
- `ACSA_SDF_Demo_Workbook.csv` is the static demo workbook sample.

The live static site still uses the root repository files, and the GitHub Actions workflow still imports from `exports/WSPTrainingManagementSystem_complete_solution.zip`.
